export interface AccountsForms {
    accForm_Code?: number,
    accForm_Name_AR?: string ,
    accForm_Name_EN?: string,
    is_transferred?: boolean,
    branch_No?: number,
    last_Transaction_Type?: string,
    last_Transaction_Date?: string
}
